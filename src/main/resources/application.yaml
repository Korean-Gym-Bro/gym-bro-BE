spring:
  application:
    name: bro

  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/fitness_db?useSSL=false&serverTimezone=Asia/Seoul
      username: root
      password: 1111

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY:}

server:
  port: ${SERVER_PORT:8080}
